$(function () {
  let checkedAmenities = [];
  let checkboxes = ('input');
  console.log("here")
  console.log(checkboxes)

  checkboxes.change(function () {
    checkedAmenities = checkboxes
      .filter(":checked")
      .map(box => box.attr('data-name'));

    $('h4').text('Hey');
  });
});
